const colors = {
  primary: '#3dc9e1',
  secondary: '#e16a3d',
  tertiary: '#2ca5d7',
  quaternary: '#38e143',
  quinary: '#913de1'
}

export const theme = {
  main: {
    button: { color: 'white', background: colors.primary, border: `solid 2px ${colors.primary}` },
    paper: { background: 'white' }
  },
  darkMode: {},
  cssTest: { color: 'red', background: '#000' }
}
